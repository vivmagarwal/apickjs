/**
 * Article service.
 *
 * Extends the default APICK core service with custom business logic.
 */
export default {
  // Uses default CRUD operations from the core service factory.
  // Add custom methods here:
  //
  // async findFeatured() {
  //   return apick.documents('api::article.article').findMany({
  //     filters: { category: 'release' },
  //     status: 'published',
  //     sort: 'createdAt:desc',
  //     pagination: { limit: 5 },
  //   });
  // },
};
