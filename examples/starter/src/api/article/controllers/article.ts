/**
 * Article controller.
 *
 * Extends the default APICK core controller with custom logic.
 */
export default {
  // Uses default CRUD actions from the core controller factory.
  // Override any action here to customize behaviour:
  //
  // async find(ctx) {
  //   const { data, meta } = await super.find(ctx);
  //   return { data, meta };
  // },
};
